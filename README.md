SELECT ENAME, DNAME FROM EMP, DEPT WHERE EMP.DEPTNO = DEPT.DEPTNO;

SELECT ENAME, DNAME FROM EMP JOIN DEPT ON (EMP.DEPTNO = DEPT.DEPTNO);

SELECT ENAME, DNAME, EMP.DEPTNO, DEPT.DEPTNO FROM EMP JOIN DEPT ON (EMP.DEPTNO = DEPT.DEPTNO);

SELECT ENAME, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E JOIN DEPT D ON (E.DEPTNO = D.DEPTNO);

SELECT ENAME, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E INNER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO);

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E INNER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO);

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E INNER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) ORDER BY SAL;

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E INNER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) WHERE SAL >= 1300 AND E.DEPTNO IN (10,30) ORDER BY SAL;

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E INNER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) WHERE SAL >= 1300 AND E.DEPTNO IN (10,30) AND DNAME LIKE '%A%' 
ORDER BY SAL;

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E RIGHT OUTER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) ORDER BY SAL;

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E LEFT OUTER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) ORDER BY SAL;

SELECT ENAME, SAL, DNAME, E.DEPTNO, D.DEPTNO FROM EMP E FULL OUTER JOIN DEPT D ON (E.DEPTNO = D.DEPTNO) ORDER BY SAL;

SELECT ENAME, DNAME FROM EMP CROSS JOIN DEPT;

SELECT * FROM SALGRADE;
SELECT * FROM EMP;

SELECT GRADE, ENAME FROM EMP, SALGRADE WHERE SAL BETWEEN LOSAL AND HISAL;

SELECT ENAME, SAL, C.DEPTNO, MEDIA FROM EMP, (SELECT DEPTNO, ROUND(AVG(SAL), 2) MEDIA FROM EMP GROUP BY DEPTNO) C WHERE EMP.DEPTNO = C.DEPTNO;
